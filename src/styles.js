import { SeaImage as backImg } from "./images";

const styles = {
  container: {
    backgroundImage: `url(${backImg})`,
    width: "100%",
    height: "100vh",
    backgroundSize: "cover",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    fontSize: "35px",
    margin: "0px",
    padding: "0px",
    // color: "#fff",
  },
};

export default styles;
